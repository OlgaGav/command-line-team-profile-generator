const Engineer = require("../lib/Engineer");

describe('Engineer', () => {
    describe('Initialize Engineer object with parameters: name, id, email', () => {
        // setup
        let githubUsername = 'testuser';
        // run
        // validation
    })
})